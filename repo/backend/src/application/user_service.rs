/// User service — CRUD operations, role assignment, and department scoping.
///
/// Manages user lifecycle (create, read, update, delete), assigns
/// and revokes roles, and enforces department-level access scoping
/// so users only see data within their authorized departments.

pub struct UserService {
    // TODO: Inject repository dependencies in Phase 2
}
